[project]
name = "dataset-gsi"
version = "0.1.0"
description = "国土地理院 指定緊急避難場所・指定避難所 dataset for fdl"
requires-python = ">=3.13"
dependencies = [
    "frozen-ducklake>=0.11.0",
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
]
